/*import { EventEmitter,
         Output
       } from '@angular/core';

export class Article {
  @Output() onArticleSelected : EventEmitter<any>;
  private editando:boolean;
  constructor(
    public nombre: string,
    public valor: string,
    public votes: number,
    public selected:boolean
  ){
    this.onArticleSelected = new EventEmitter();
    this.editando = false;

  }
  public toggleSelected(article:Article):void{
    this.selected = !this.selected;
    this.onArticleSelected.emit('Evento seleccionado');
  }

}
*/
